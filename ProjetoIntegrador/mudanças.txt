adicionado o carrossel;
arrumado o cadastro de itens;
além de umas coisas no banco de dados;

lembrete, mudar umas coisas do php pq o banco de dados foi alterado...eu acho
 
CREATE TABLE USUARIO(
    id_usuario SERIAL PRIMARY KEY,
    loginUsuario VARCHAR(255),
    senha VARCHAR(255),
    email VARCHAR(255),
    telefone INT,
    tipo CHAR
);

CREATE TABLE PRODUTOS(
    id_produto SERIAL PRIMARY KEY,
    url_imgProd VARCHAR(255),
    nome VARCHAR(255),
    preco INT,
    descricao VARCHAR,
	tipo CHAR
);

ALTER TABLE PRODUTOS ALTER COLUMN preco TYPE numeric(5,2);

select * from  produtos


INSERT INTO PRODUTOS(nome,preco,descricao,tipo) VALUES ('vassoura',24.67,'dsadasdassdassdadasdsadsadasdsadas','P')